services:
  gemini-web2api:
    build: .
    container_name: gemini-web2api
    ports:
      - "8081:8080"
    environment:
      PORT: 8080
    volumes:
      - ./data:/app/config
    restart: unless-stopped
